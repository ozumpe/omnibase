"""sis — the self-improving server engine.

The package holds the *immutable* engine code. Runtime-mutable artifacts
(the live target, generated candidates, the episodic log) live under the
sibling ``runtime/`` directory; see :mod:`sis.paths`.
"""
